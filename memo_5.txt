---------------------------------- <데이터 전처리의 유의사항> ------------------------------

데이터를 잘못 추출 or 데이터 전처리과정에서 이상치(outlier)를 반영
데이터가 오염

결측치?

-> 문제 있는 데이터는 평균을 구하나, (평균의 함정 발생 시) 삭제해도 좋다

-----------------------------------------------------------------------------------------

activation : 활성화 함수 -> 성능 향상

actvation = 'relu'
parameter한 연산결과가 모두 양수인 값만 이후의 layer에 전달할 수 있도록
음수인 값을 모두 0으로 취급해
없는 데이터 취급한다

linear(선형 회귀식) : 그 값 그대로 쓰겠다

-----------------------------------------------------------------------------------------
dacon
ai factory
ai 경쟁대회
-----------------------------------------------------------------------------------------
verbose = 0 : 침묵
 -> 콘솔창에 일련과정을 보여주지 않아, 빠르게 결과를 출력
 -> default : 1 (과정이 보임)
 -> 2 : 진행과정(epochs값과 각 시간)은 보이나 실행바가 안보임
 -> 3 : epochs값만 보임(각 시간은 안보임)

------------------------------------------------------------------------------------------

data는 training data, test data로 분리해야 하고,
각각은 가능한 한 전체에서 추출해
일부 데이터만 반영되는 것을 방지.

------------------------------------------------------------------------------------------

Validation : 훈련과정 중에 실행 도중의 지표를 확인
-> 훈련 데이터중에 일부 데이터를 뺀다

val loss가 최소인 x값 -> 최소의 loss값을 구할 수 있음 = 최적의 weight
훈련을 중간에 끊는다

loss <- mse, rmse, rmsle, mae
r2

from tensorflow.keras.callbacks import EarlyStopping


하이퍼 파라미터에서 무의미
----------------------------------------------------------------------

이름 명명규칙
cammel case             ex : EarlyStopping
snake case              ex : Early_stopping